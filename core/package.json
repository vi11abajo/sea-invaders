{
  "name": "@sea-invaders/core",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic game core shared by the Sea Invaders app and server",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "react-native": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "~22.20.1",
    "typescript": "~6.0.3",
    "vitest": "4.1.9"
  }
}
